const io=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting){x.target.classList.add('in');io.unobserve(x.target);}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const parallaxEls=[...document.querySelectorAll('[data-parallax]')];function onScroll(){const y=scrollY;parallaxEls.forEach(el=>{const f=parseFloat(el.dataset.parallax)||.2;el.style.transform=`translateY(${y*f}px)`;});}addEventListener('scroll',onScroll,{passive:true});onScroll();